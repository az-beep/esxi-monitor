{
  "name": "vm-frontend",
  "version": "1.0.0",
  "description": "Frontend для мониторинга ESXi",
  "scripts": {
    "start": "live-server --port=3000 --open=./login.html --host=localhost --cors",
    "dev": "live-server --port=3000 --open=./login.html --host=localhost --cors"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}